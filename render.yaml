services:
  - type: web
    name: fry-counter
    runtime: python
    buildCommand: echo "No build step needed"
    startCommand: python server.py
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
